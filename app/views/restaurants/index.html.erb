<div class="container">
  <div class="row">
    <div class="col-6">
            <h1>our restaurants</h1>
      <% @restaurants.each do |restaurant| %>
        <h3><%= link_to restaurant.name , restaurant_path(restaurant) %> specialised in <%= restaurant.category %> food</h3>
      <% end %>
      <hr>
      <%= link_to 'add new restaurant', new_restaurant_path  %>
    </div>
  </div>
</div>
